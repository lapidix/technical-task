# AWS Lambda Deployment Configuration
# Copy this file to .env.deploy and replace with actual values

# ACM SSL Certificate ARN (ap-northeast-2 region)
# SSL/TLS certificate for HTTPS
# Get from AWS Certificate Manager
ACM_CERTIFICATE_ARN=arn:aws:acm:ap-northeast-2:YOUR_ACCOUNT_ID:certificate/YOUR_CERT_ID

# Custom Domain (connected to API Gateway)
# Example: api.yourdomain.com, task.yourdomain.com
CUSTOM_DOMAIN=your-domain.com

# AWS Credentials (optional - can be set via aws configure)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=ap-northeast-2
